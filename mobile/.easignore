# Keep the EAS upload small — local caches/logs must not ship.
.expo/
.expo-shared/
dist/
web-build/
node_modules/
npm-debug.*
yarn-error.*
*.log
.git/
ios/
android/
coverage/
.vscode/
.idea/
.env
.env*.local
*.jks
*.keystore
# Keep google-services.json in the EAS upload when present (required for Android FCM push).
# Still gitignored locally if you prefer secrets via CI — place the file before `eas build`.
